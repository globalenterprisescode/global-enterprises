export interface Enquiry {
  id: string;
  name: string;
  phone: string;
  email: string;
  requirement: string;
  created_at: string;
}

export interface DateRange {
  startDate: string;
  endDate: string;
}